# Test
pytest>=8.0.0
httpx>=0.27.0          # FastAPI test client için
# Uygulama
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-docx==1.1.2
python-multipart==0.0.9
pydantic==2.9.0
# RAG
chromadb>=1.5.0
pdfplumber>=0.11.0     # PDF metin çıkarma
# Katman B embedding — gerçek anlamsal model (multilingual-e5).
# DİKKAT: torch'u bağımlılık olarak getirir (~1 GB disk, ~400 MB RAM).
# Model indirilemezse SimpleHashEmbedding fallback'ine düşülür (düşük kalite).
sentence-transformers>=3.0.0
# langchain==0.2.0
# langchain-community==0.2.0
# Ortam değişkenleri
python-dotenv>=1.0.0
# Katman C — Semantik Analiz
google-genai>=1.0.0   # Birincil: Gemini 2.5 Flash
groq>=0.11.0          # Yedek: Llama 3.3 70B via Groq (ucretsiz tier)
